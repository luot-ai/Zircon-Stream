1. 12/24：让LSPP的访存请求优先于SE的请求，IPC从0.73增到1（因为LSPP的指令被阻塞会导致ROB满，后续指令无法发出）
   1. TODO：奇怪的点是cache-trace到的l1-miss多了很多，先不管吧
2. 12/30：MNK32下，由于L1只能装512个数，这是完全装不下B的，B数组每次都要去L2请求，这是极大的开销，IPC很低，所以直接改成L2最好
3. 01/04：  
   1. 循环展开32次(32bit)：矩阵乘90846 83%(最好83)【111条指令 IPC1.25，比咱们多了44条lw指令】，转置+11449（10%）累计到102295【还应该加上分支跳转基本块5和7，累积到109272】
      1. 基本块把b数组进行了一些copy(12) -> mem 以及放到reg里(20)，和44条lw指令可以对上
      2. 感觉还是有点不理解，先不管
   2. 咱们这边 44331
   
   省lw指令，44/111
   l1只能装512字，ABC都是1024，会有不少cache miss
      咱们不走L1
         B数组走L2，L2复用
         A数组走AXI，stream buffer复用